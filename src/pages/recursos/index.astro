---
import Layout from '@/layouts/Layout.astro'
import { blogPostData } from '@/data/blogPostData'
---

<Layout titlePage='Kit de Datos Abiertos' descriptionPage='' currentUrl='' isWhiteBg={true}>
  <section class='bg-blue-accent'>
    <div class='section-container py-16'>
      <h3 class='section-title mb-8 text-white'>Centro de Recursos</h3>
      <p class='section-subtitle text-zinc-200'>
        Encontrarás herramientas, guías y conocimientos esenciales diseñados para ayudarte a
        aprovechar al máximo el potencial de los datos.
      </p>
    </div>
  </section>

  <section class='section-container'>
    {
      blogPostData.map(post => {
        return (
          <a href={post.postUrl}>
            <article class='grid cursor-pointer place-content-center place-items-center gap-8 border-b py-8 transition-transform hover:scale-[1.01] md:grid-cols-[1fr,max-content]'>
              <img
                class='order-1 w-full max-w-60 rounded-xl sm:max-w-xs md:order-2'
                src={post.imageUrl}
                alt={post.title}
              />
              <div class='order-2 md:order-1'>
                <h3 class='section-title mb-8'>{post.title}</h3>
                <p class='section-subtitle mb-8'>{post.description}</p>
                <footer class='fonf-modium tent-medium text-se'>
                  <span>
                    {post.tags.map(tag => {
                      return <span>{tag}</span>
                    })}
                  </span>
                  <span> | </span>
                  <span>{post.date}</span>
                </footer>
              </div>
            </article>
          </a>
        )
      })
    }
  </section>
</Layout>
